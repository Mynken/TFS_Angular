
export enum Gender {
    None,
    Female,
    Male
}

export enum Status {
    New = 1000,
    InWork = 1001,
    Finished = 1002
}

export enum Validation {
    Required = 2000,
    MinLength = 2001,
    MaxLength = 2002,
    Custom = 2003
}

export enum BugStatus {
    Active = 3000,
    Closed = 3001,
    New = 3002,
    Resolved = 3003
}
